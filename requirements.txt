# Core webhook ingestion (WhatsApp Cloud API)
fastapi>=0.110
uvicorn[standard]>=0.29

# Optional: LLM-backed per-person summaries and meeting briefings.
# Everything except the narrative summary works without this installed.
anthropic>=0.40

# Dev / tests
pytest>=8.0
httpx>=0.27
